{#
 # This file is part of Galette Auto plugin (https://galette.eu).
 # SPDX-FileCopyrightText: Copyright © 2009-2026 The Galette Team
 # SPDX-License-Identifier: GPL-3.0-or-later
 #}

{% extends 'elements/list.html.twig' %}

{% set field = obj.getField() %}

{% set nb = list|length %}

{% block infoline %}
    {% set infoline = {
        'label': count_label,
        'route': {
            'name': 'propertyFilter',
            'args': {'property': field}
        }
    } %}
    {{ parent() }}
{% endblock %}

{% block infoline_actions %}
    <a
        class="ui tiny labeled icon button"
        href="{{ url_for("propertyAdd", {"property": obj.getRouteName()}) }}"
    >
        <i class="plus circle green icon" aria-hidden="true"></i>
        {{ add_text }}
    </a>
{% endblock %}

{% block header %}
    {% set columns = [
        {'label': field_name}
    ] %}
    {{ parent() }}
{% endblock %}

{% block body %}
    {% for o in list %}
        {% set edit_link = url_for("propertyEdit", {"property": obj.getRouteName(), "id": o.getId()}) %}
    <tr>
        <td data-col-label="{{ field_name }}">
            <a
                href="{% if obj.hasDetails() %}{{ url_for("propertyShow", {"property": obj.getRouteName(), "id": o.getId()}) }}{% else %}{{ edit_link }}{% endif %}"
            >
                {{ o.getValue() }}
            </a>
        </td>
        <td class="center actions_row collapsing">
            <a href="{{ edit_link }}" class="tooltip action">
                <i class="ui edit icon"></i>
                <span class="visually-hidden">{{ _T("Edit %property", "auto")|replace({"%property": o.getValue()}) }}</span>
            </a>
            <a
                class="delete tooltip"
                href="{{ url_for("removeProperty", {"property": obj.getRouteName(), "id": o.getId()}) }}"
            >
                <i class="ui red trash icon"></i>
                <span class="visually-hidden">{{ _T("%property: remove from database", "auto")|replace({"%property": o.getValue()}) }}</span>
            </a>
        </td>
    </tr>
    {% else %}
        <tr><td colspan="3" class="emptylist">{{ _T("no record found", "auto") }}</td></tr>
    {% endfor %}
{% endblock %}
